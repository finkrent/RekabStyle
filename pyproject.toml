[project]
name = "mimo-shop"
version = "0.1.0"
description = "Backend of an online shop (Django + DRF + PostgreSQL)"
requires-python = ">=3.12"
dependencies = [
    "django>=5.1",
    "djangorestframework>=3.15",
    "psycopg[binary]>=3.2",
    "python-dotenv>=1.0",
    "requests>=2.32",
    "pillow>=10.0",
    "djangorestframework-simplejwt>=5.5.1",
]
